{
  "name": "mini-multiplayer-editor",
  "version": "0.0.0",
  "private": true,
  "description": "A small real-time collaborative code editor (Yjs + CodeMirror 6 + y-websocket).",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n server,client -c green,cyan \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "start": "node server/src/index.js"
  },
  "devDependencies": {
    "concurrently": "10.0.5"
  }
}
